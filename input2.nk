fn test() -> i64{
	let:i64 n = 10;
	return n * n;
}
fn test2(i64:a,i64:b) -> i64{
	return a*b;
}
fn main() -> i64{
    let:i64 n = 1; 
	let:i64 a = 10;
	let:i64 b = 11;
	let:i64 c = a+b;
    let:i64 i = 1;
    let:i64 sum_of_squares = 0;

    for(i -> n :: 1) {
        sum_of_squares += i ;
    
	}
	let:i64 t=test();
	let:i64 t2=test2(a,test());
    return t2;
}

